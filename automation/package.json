{
  "name": "aharsetu-e2e-automation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --parallel --timeout 120000 'tests/**/*.spec.js' --reporter mochawesome --reporter-options reportDir=reports/html,reportFilename=execution-report,quiet=true",
    "test:selenium": "npm run test",
    "load:test": "node load/loadTest.js",
    "security:test": "node security/securityScan.js",
    "ci:e2e": "npm run test:selenium && node utils/excelReporter.js && node utils/htmlReporter.js && node utils/reportSummary.js",
    "ci:all": "npm run test:selenium && npm run load:test && npm run security:test && node utils/excelReporter.js && node utils/htmlReporter.js && node utils/reportSummary.js"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "chromedriver": "^115.0.0",
    "exceljs": "^4.3.0",
    "loadtest": "^5.3.0",
    "mkdirp": "^3.0.1",
    "selenium-webdriver": "^4.11.0",
    "dotenv": "^16.1.4"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3"
  }
}
